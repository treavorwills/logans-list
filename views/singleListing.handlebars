{{> searchAndCat}}

<section class="bg-white py-8">
    <div class="container mx-auto w-full flex flex-wrap justify-center pt-4 pb-12 ">
        <!-- <div class="flex flex-wrap justify-around"> -->
        <div>
            <p class="mx-10 flex justify-center text-2xl font-bold">{{{name}}}</p>
            <p class="text-gray-900 mx-10 flex justify-center text-xl font-bold"> $ {{{price}}}</p>
        </div>
        <!-- </div> -->
        <div class="flex flex-auto w-full justify-around">
            <div class="w-full md:w-1/3 xl:w-1/4 flex container justify-center p-2.5">
                <img class="hover:grow hover:shadow-lg rounded-lg" src={{{image_url}}}>
            </div>
        </div>
        <div>
            <div class="pt-3 flex-col items-center justify-center">
                <div class="flex justify-around">
                    <p class="text-xl font-bold pt-0.5">Description:  </p><p class="pt-1 pl-1.5">{{{description}}}</p>
                </div>
            </div>
            <div class="flex-col items-center justify-center flex-wrap">
                <p class="text-xl font-bold pt-0.5">Contact Info: </p><p>{{user.email}}</p>
            </div>
        </div>
    </div>
    </div>

</section>